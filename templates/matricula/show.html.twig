{% extends 'base.html.twig' %}

{% block title %}Matricula{% endblock %}

{% block body %}
    <h1>Matricula</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ matricula.id }}</td>
            </tr>
            <tr>
                <th>FechaInscripcion</th>
                <td>{{ matricula.fechaInscripcion ? matricula.fechaInscripcion|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_matricula_index') }}">back to list</a>

    <a href="{{ path('app_matricula_edit', {'id': matricula.id}) }}">edit</a>

    {{ include('matricula/_delete_form.html.twig') }}
{% endblock %}
